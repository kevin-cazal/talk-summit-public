SECTION "main", ROM0
Start:
    ld hl, $c3a0
    ld [hl], $93        ; T
    inc hl
    ld [hl], $87        ; H
    inc hl
    ld [hl], $80        ; A
    inc hl
    ld [hl], $8D        ; N
    inc hl
    ld [hl], $8A        ; K
    inc hl
    ld [hl], $92        ; S
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $FA        ; 4
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $8B        ; L
    inc hl
    ld [hl], $88        ; I
    inc hl
    ld [hl], $92        ; S
    inc hl
    ld [hl], $93        ; T
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $8D        ; N
    inc hl
    ld [hl], $88        ; I
    inc hl
    ld [hl], $8D        ; N
    inc hl
    ld [hl], $86        ; G
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $81        ; B
    inc hl
    ld [hl], $94        ; U 
    inc hl
    ld [hl], $86        ; G
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $8E        ; O
    inc hl
    ld [hl], $91        ; R
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $85        ; F
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $80        ; A
    inc hl
    ld [hl], $93        ; T
    inc hl
    ld [hl], $94        ; U 
    inc hl
    ld [hl], $91        ; R
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $E6        ; ?
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $8A        ; K
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $95        ; V
    inc hl
    ld [hl], $88        ; I
    inc hl
    ld [hl], $8D        ; N
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $82        ; C
    inc hl
    ld [hl], $80        ; A
    inc hl
    ld [hl], $99        ; Z
    inc hl
    ld [hl], $80        ; A
    inc hl
    ld [hl], $8B        ; L
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $8F        ; P
    inc hl
    ld [hl], $88        ; I 
    inc hl
    ld [hl], $93        ; T
    inc hl
    ld [hl], $84        ; E
    inc hl
    ld [hl], $82        ; C
    inc hl
    ld [hl], $87        ; H
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $92        ; S
    inc hl
    ld [hl], $94        ; U 
    inc hl
    ld [hl], $8C        ; M
    inc hl
    ld [hl], $8C        ; M
    inc hl
    ld [hl], $88        ; I 
    inc hl
    ld [hl], $93        ; T
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $F8        ; 2 
    inc hl
    ld [hl], $F6        ; 0
    inc hl
    ld [hl], $F8        ; 2
    inc hl
    ld [hl], $FC        ; 6
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
    ld [hl], $7F        ; space
    inc hl
WaitStart:
    ldh a, [$fff8]
    bit 3, a
    jr z, WaitStart
    ret
